#STM32 #HAL库 

>*句柄是一个**结构体指针**。*
>
>包含了外设的所有配置信息和状态。

如：

```
typedef struct {
    ADC_TypeDef *Instance;
    ADC_InitTypeDef Init;
    HAL_ADC_StateTypeDef State;
    // ... 其他成员
} ADC_HandleTypeDef;
```

>说白了就是 #标准库 里面折磨人的结构体。